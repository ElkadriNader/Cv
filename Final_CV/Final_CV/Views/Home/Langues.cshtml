@model IEnumerable<Final_CV.Models.Language>

@{
    ViewBag.Title = "Langues";
    Layout = "~/Views/Shared/_LayoutHome.cshtml";
}

<h2>Langues</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Niveau)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Logo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Niveau)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Logo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LangaugeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LangaugeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LangaugeID })
        </td>
    </tr>
}

</table>
